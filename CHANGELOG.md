# Changelog

## v3.2 - 卷心菜投手与磁力菇（2026-08-07）

### 将王博士改成报纸将王 📰
- 改名：全项目「将王博士 / 将王博士机甲」统一改成**报纸将王**（代码注释、
  Tooltip、帮助面板、状态栏、README、bot 脚本一并跟上）
- 外观：机甲 🤖 换成他手里举着的一张大报纸 📰（微微倾斜，低头时跟着压下来），
  脑袋仍是那颗巨型僵尸头
- **只召唤报纸僵尸**：`waveLineup` 从「普通 → 路障 → 铁桶 → 报纸 → 撑杆 → 铁门
  从弱到强轮换」改成一水儿的 newspaper —— 他手里就一摞报纸，放出来的自然全是报纸僵尸
- 血量不变：第 1 关仍是 6400 万，仍随关卡线性上涨
- 「机甲」这个说法在注释和 CSS 里也一并改掉了，免得留着自相矛盾
- 实测：血量 64000000 未变；一波 20 只全是 newspaper；场上僵尸类型只有 newspaper；
  状态栏显示「第 1 关 · 报纸将王 …」；旧的 .boss-mech 元素已清零

### 核爆樱桃顶上那颗改用樱桃炸弹的樱桃
- 原来是自绘的一颗红球 + 一片小绿叶（`.nc-berry` / `.nc-leaf`），现在直接换成
  樱桃炸弹那颗 🍒，两者长得一模一样
- 新的 `.nc-cherry` 尺寸用 em，跟着 `.nuke-cherry` 一起缩放 —— 种子卡上和
  棋盘上都不走样
- 植物图标和种子卡的 HTML 一起改，旧的 nc-berry / nc-leaf 已全部清干净

### 红樱桃改用樱桃炸弹的样子
- 红叶素打出的小红樱桃原来是自己画的一颗红球 + 一根小绿梗，现在直接用
  樱桃炸弹那颗 🍒（两颗连梗的樱桃），和植物本体一个样
- Projectile 的弹道表新增 content 字段（`createDOM` 用它填内容），
  这样弹种可以直接指定一个 emoji 当造型
- 对应的 CSS 从「自绘红球」改成放 emoji 的容器（背景 none，只留红色发光）
- 实测：红叶素期间打出的 redcherry 子弹，DOM 内容就是 "🍒"，
  和樱桃炸弹本体完全一致；伤害仍是 1000

### 新增道具：手套 🧤
- 绿色手套（整只 CSS 画的：连指手套 + 三道指缝 + 黄袖口），和铲子一样是道具卡
- 点第一下抓起那一格栈顶的植物，点第二下放到任意一格
- **搬的是同一个 Plant 对象**，不是拆了重种 —— 所以它一身的状态全都跟着走：
  融合星级、超级机枪的连发数、火炬树桩的火色、毁灭菇涨上去的伤害、
  阳光帝果攒的球…… 一点都不丢
- 倭瓜的「老家」坐标会跟着更新，否则它砸完会跳回搬家前那一格
- 蓝草坪照样只收房子，别的植物放不进去，会留在手里
- Esc、切换到别的种子卡或铲子，都会先把手里那株放回原处，不会凭空消失
- 被抓在手里的那株半透明 + 绿色虚线框，看得出它离开了地面
- 抓起来和点空格都**不弹提示** —— 光标换成那株植物、它自己半透明带虚线框，
  已经够看出来了；点空格就是静默无事发生
- 实测：抓起 9 连的超级机枪搬到别处 → 还是同一个对象、连发数仍是 9；
  往蓝草坪放被拒且仍握在手；Esc 放回原处；抓起 / 点空格都不弹任何提示

### 通关奖杯 🏆 与关卡推进
- 新增关卡概念：`game.level` 从第 1 关起，状态栏显示「第 N 关」
- 打死将王博士后，棋盘正中掉下一个金奖杯（`classes/Trophy.js`）：从天上落下弹两下，
  一直闪着金光等你点；点一下金光炸开、杯子越涨越大，涨满后进入下一关
- 掉奖杯期间**不再弹胜利遮罩** —— 遮罩会盖住棋盘，杯子就点不到了
- 下一关博士血量随关卡线性上涨：第 1 关 6400 万、第 2 关 1.28 亿、第 3 关 1.92 亿…
- 回首页会把关卡归 1；restart / goHome 都会清掉残留的奖杯
- 奖杯的点击事件会 stopPropagation，否则棋盘那层会把它当成「往这一格种植物」
- 实测：打死博士 → 奖杯出现且不弹遮罩 → 点击变大 → 进第 2 关（博士 1.28 亿）→
  连着再通一关到第 3 关（1.92 亿）→ 回首页归 1，无残留奖杯

### 倭瓜能回头砸
- 侦测范围从「前方三格」改成**前后三格**，挑离得最近的那只；起跳方向跟着目标走，
  背后来的就往后跳（撑杆跳跳过去、被磁力菇 / 原始豌豆推到身后的僵尸，
  以前倭瓜完全不理）
- 起跳前会再确认一次方向 —— 盯人那 3 秒里目标可能已经绕到另一边
- 向后跳时整个瓜左右翻过来（`.squash-backward`），看得出它在朝背后使劲
- 落点范围本来就是以瓜为中心左右各半格，向后砸不用另外处理
- 实测：背后一格 → 压扁并回原位（dir=-1）；前面一格 → 照旧（dir=1）；
  前后都有 → 砸更近的那只；背后 4 格 → 超范围不理

### 倭瓜大招：随机砸死 5 只再回原位
- 绿叶素从「跳过盯人那 3 秒、立刻起跳」改成**满场乱跳**：把场上僵尸洗牌后
  随机挑 5 只，挨个跳过去砸死（跨行也跳），砸完跳回原来那一格接着蹲
- 场上不够 5 只就有几只砸几只；一只都没有时给提示，不会乱跳
- 队列里的目标可能被别的植物先打死，跳到它时会自动跳过、找下一只
- 大招会跨行，所以「老家」现在 x 和 y 都记；回家时一并还原
- 盯人盯到一半吃绿叶素也会立刻转成大招
- 落点冲击圈抽成 smashFx，普通砸和大招砸共用
- 实测：场上 12 只 → 正好砸死 5 只并回到原位；只有 3 只 → 砸完 3 只也回原位；
  空场 → 原地不动并提示；大招之后普通流程（盯 3 秒 → 前跳 → 砸 → 回家）照常

### 卷心菜投手：普通攻击也改成越障抛射
- 普通攻击从「贴地平射的直线子弹」改成和大招一样的抛物线抛射 —— 从高空越过
  坚果墙、其他僵尸和一切护甲，直接砸在同行最靠前那个目标头上，仍然 40 点
- 复用公共的 `Lob.js`（大招原本就有自己那套抛射，这次没动它）
- 顺手删掉已经没人引用的 `cabbage` 直线弹种及其 CSS（含它专用的自转动画）
- 实测：隔着一堵坚果墙照样砸到后面的僵尸（掉血 80 = 两颗 ×40）；
  场上不再产生 cabbage 类型的直线子弹；大招照旧秒杀

### 博士低头时植物会自己打他
- 以前场上没僵尸时植物集体停火 —— 博士不在 `game.zombies` 里，
  `hasEnemyInRow` / `hasAnyEnemy` 都看不见他，于是他低着头也没人打
- 这两个判断现在把**低头（或被冻住）的博士**算作一个敌人（新增
  `bossTargetInRow`：他那台机甲很高、纵向压着好几行，所以按行区间是否重叠来判）
- 抛射类（西瓜 / 玉米 / 超级投手）的 `frontTarget` 同样处理：同行没有僵尸时
  就抛给低头的博士。顺手把它的返回值从「僵尸」改成 lob 用的目标描述符，
  三个调用方跟着改
- 超级投手的弹射砸到博士就直接落地 —— 他不在任何一「排」里，没得弹
- 实测（场上一只僵尸都没有）：博士低头 → 2.6 秒里打出 149 发、掉血 150；
  他没低头 → 掉血 0；被冻住 → 照样挨打

### 超级投手大改：三合一抛射 + 排内弹射
- 外观从 🎯 换成**卷心菜 / 西瓜 / 玉米三个投手摞在一起**（整摞 CSS 画的：
  上层卷心菜、中层条纹西瓜、下层玉米，底下一截树桩）
- 攻击从「直线穿透弹」改为抛射：抛出去的也是三合一（一颗卷心菜 + 一颗西瓜
  + 一块玉米叠成一坨），落点 240 点
- **排内一直弹**：砸中一只之后弹到同一排的下一只接着砸，**一直弹下去**，
  把这一排弹到一只不剩才掉到地上。优先弹给「刚砸过的那只之外」的僵尸；
  整排只剩一只时就在它身上反复砸。每跳实打实扣 240，所以这一排迟早清空、
  弹射自然收敛（第一版限制了「同一发不重复砸同一只」，按要求改成无限弹）
- 找下一只的顺序是「先往右找最近的，右边没有再往左找」，所以它会顺着一排
  推过去，推到头再回头补
- 绿叶素大招：对全场每一个目标（含将王博士）各抛一发**巨大**的三合一，
  穿越一切障碍物，2400 点
- Lob.js 补三个钩子支撑这套：`from`（起抛点，弹射要从上一个落点接着抛）、
  `html`（抛射物内部结构，给复合造型用）、`after`（落点回调，用来接着弹）
- 实测（单发隔离）：同排 4 只各掉 240、别的排 0；同排只有 1 只时正好掉 240
  不重复弹；同排无僵尸时不发射；大招三只各 2400、博士也 2400

### 西瓜改成圆西瓜，大招只大一圈；所有大招都打得到将王博士
- 西瓜不再用 🍉（那是切开的一牙，不是整颗），改成 CSS 画的**圆西瓜**：
  绿皮 + 放射状深绿条纹 + 高光，看着是个球
- 普通西瓜 30 → 34px；大招的西瓜从 74px 的「巨型」收成 **46px**，
  只比普通那颗大一圈（红叶素的巨型红樱桃仍是 74px，那是另一个大招）
- `Lob.collectTargets` 把将王博士也算成一个目标 —— 场上只剩他的时候，
  西瓜 / 玉米投手的大招不会再以为「没有目标」直接哑掉
- 卷心菜大招、磁力菇大招同样把博士算进目标；毁灭菇和寒冰菇原本就打得到他
- 砸到博士仍然走 `boss.takeDamage`，所以他没低头 / 没被冻住时照样无敌
- 卷心菜对博士的伤害改用 `BOSS_SENTINEL_DAMAGE`（它对僵尸是直接清血的秒杀，
  照搬会一发打光博士）
- 实测（场上只剩博士、他正低头）：西瓜 700 / 卷心菜 800 / 磁力菇 17000 /
  毁灭菇 3600 / 玉米投手 100；博士无敌期一律 0

### 豌豆炸弹：100 颗必发满
- 有没有僵尸都照炸，100 颗一颗不少。炸弹本来就没有僵尸判定，之前发不满是被
  子弹上限卡的 —— 子弹现在只有打到僵尸才消失，没僵尸时场上很容易一直顶着
  700 上限，`spawnProjectile` 返回 null，这一炮就哑掉大半
- `spawnProjectile` 新增 force 参数，无视上限硬发，只有豌豆炸弹在用
- 实测：空场无僵尸射出 100 颗；子弹已顶满 700 时照样射出 100 颗（700 → 800）；
  方向分布 左 50 / 右 50 / 上 48 / 下 50，四面八方都有

### 子弹撞边一直反弹
- 子弹打到棋盘四条边不再回收，而是把速度反向弹回来接着飞，**一直弹下去**
  （`Projectile.bounce`）
- 子弹**不和植物做任何碰撞判定** —— 碰撞只跑 `game.zombies` 和游荡者，
  从植物身上飞过去什么也不会发生。唯一的消失方式就是打到僵尸
- 每弹一次重置 `_hitBoss` / `_frozeBoss`，所以反弹回来还能再打博士一次
- 纵向反弹只对真在上下移动的子弹生效；没有 vy 的子弹（比如加特林出生点带
  随机纵向偏移）贴到上下边只是夹回来，不会凭空获得纵向速度
- 700 上限的行为：满员时**不发新子弹**，而不是顶掉场上最老的那颗。中途试过
  顶掉最老的，结果是子弹会在半空中平白消失（看起来像撞到植物没了），已撤回。
  代价是穿透弹打到僵尸也不消失、会一直占着槽位，攒满之后就发不出新子弹了
- 实测：穿过整行 9 株植物（含坚果墙）毫发无伤；满屏 60 株植物中飞 900 帧仍在弹；
  打到僵尸即消失；满 700 颗后新子弹被拒但场上老子弹一颗不少

### 新植物
- 新增卷心菜投手 🥬（cabbagepult）：每 0.8 秒向同行投出一颗卷心菜，造成 40 点伤害
- 卷心菜投手 🥬 + 绿叶素：向场上每一个僵尸（含游荡者）各抛出一颗巨大卷心菜，
  抛物线从高空越过坚果墙 / 其他僵尸等障碍物，落点绕过报纸与铁门护盾判定，
  直接造成「无数点」伤害；场上无僵尸时给出提示而不是静默无反应
- 卷心菜子弹外观：绿色渐变球体 + 反向旋转动画；大招卷心菜边飞边旋转，落地绿色溅射
- 新增磁力菇 🧲（magnetshroom）：两段式磁场，先斥后吸。普通攻击每 1.5 秒打出一次
  斥力脉冲，把场上所有僵尸（含游荡者）往右轰 80px，再持续把它们纵向吸到自己
  所在的这一行，把整片草坪的进攻压缩成一条线
- 磁力菇 🧲 + 绿叶素：先把全场僵尸一口气击退 280px，再用 0.5 秒吸成一条直线，
  最后引爆磁场 —— 先扒掉路障 / 铁桶 / 铁门 / 报纸等金属护甲，再对每个僵尸造成
  17000 点伤害（正好是本作僵尸满血），游荡者被轰回右侧重新爬
- 磁力菇外观：紫色脉动光晕，斥力脉冲有向外扩散的紫环，被吸僵尸带紫色描边，
  大招目标行整条亮起磁场与冲击波

- 新增超级机枪 🪖（supermg）：戴钢盔的豌豆，每 0.5 秒泼出 6 颗豌豆、每颗 20 点伤害；
  每打满 5 轮（约 2.5 秒）自动开一次大招，向前扇形喷出 150 颗散射豌豆，每颗同样 20 伤。
  这个大招是自动触发的，和绿叶素无关
- Projectile 新增 mgpea / scatterpea 两种弹道，并支持 `vy` 纵向速度（散射用，飞出上下边界即回收）
- spawnProjectile 现在返回创建出的弹体，省掉「取 game.projectiles 末位」的写法

- 新增超级电能机枪豌豆 ⚡（elecmg）：青色豌豆 + 和超级机枪同款钢盔。每 0.5 秒
  泼出 6 颗电能弹，**无限贯穿** —— 命中不消失，一路穿到屏幕右边，一梭子清空整行；
  对普通僵尸是无限点伤害。每打满 5 轮自动开一次大招，扇形喷出 150 颗电能散射弹
- 电能弹打将王博士时 1800 起步：Projectile 新增 `bossDamage` 字段（弹种自带的
  「打博士专用伤害」基数），boss 结算优先取它，并和别的子弹一样乘上逐发 +15%
  的递增倍率、封顶 18000
- 超级电能机枪豌豆 ⚡：每开一次散射大招，这一株的常规连发数永久 +1
  （6 → 7 → 8 …），植物右上角显示「N 连」徽章；超过 8 颗自动另起一条弹链
  上下铺开，避免弹链一路排到屏幕右边
- Projectile 新增 elecpea / elecscatter 两种弹道；新增 `classes/ElectricMG.js`
- 外观：青色渐变弹体 + 电弧闪烁动画，蓝白色枪口焰（`.elec-muzzle`）；
  植物本体用 hue-rotate 把豌豆染青、再把钢盔转回原色

- 新增寒冰菇 🍄❄️（iceshroom）：头顶竖着一支冰锥（CSS 画的三角，不是冰块 emoji）
  的蘑菇。种下后每 5 秒寒冰爆炸一次，对全场僵尸造成 2 点伤害并把它们全部冻住
  5 秒；游荡者打不死，但速度会被归零
- 冻结复用僵尸自己的 stunTimer，但视觉上和眩晕分开：`.iced` 排在 `.stunned`
  之后，盖掉金光抖动和头顶的 💫，改成**脚下一排冰锥** + 冰蓝辉光
- 寒冰菇的爆炸和寒冰射手的子弹都能把将王博士**停住**（`Boss.freeze`）：冻住期间
  相位和放僵尸计时全停，既不放僵尸也不换阶段，而且他停在原地照样可被攻击
  （`vulnerable` 把冻住也算进去，避免冻在放僵尸阶段时变成打不到的死局）。
  寒冰子弹碰到他就冻 2 秒，不看他低没低头
- 寒冰菇 🍄 + 绿叶素：冰暴 —— 全屏冰雾，冻结时间翻倍到 10 秒，并且这一株的爆炸
  间隔永久缩短 1 秒（5 → 4 → 3 …，最低 1 秒），植物右上角显示「N 秒」徽章。
  缩短存在植物身上，铲掉就没了，同格叠种各算各的
- 外观：冰环扩散动画（`.ice-ring`）、全屏冰雾（`.ice-storm`）、被冻僵尸的冰壳
  （`.zombie.iced`）、间隔徽章（`.ice-badge`）

- 新增毁灭菇 ☠️（doomshroom）：灰色菌柄 + 黑色伞盖（emoji 里没有黑蘑菇，
  整株用 CSS 画的）。每 5 秒爆炸一次，掀起一朵紫色蘑菇云，对全场僵尸造成
  1800 点伤害；将王博士也在打击范围内（没低头 / 没被冻住时伤害被他自己吞掉）
- 毁灭菇 ☠️ + 绿叶素：这一株的爆炸伤害永久 +1800（1800 → 3600 → 5400 …），
  蘑菇云直径跟着长一圈、全屏紫闪更亮，**没有上限**，喂几次涨几次；
  植物右上角显示当前伤害徽章
- 毁灭菇 ☠️ 种下后会肉眼可见地一直长大：每秒 +4%，每开一次大招再窜 0.5 倍，
  不封顶。整株是按 em 画的，所以放大改的是 `.plant` 的 font-size —— 不能动
  transform，那上面挂着出场和漂浮两个动画，会把内联 transform 盖掉
- 蘑菇云是 CSS 画的：底下一根往上顶的云柱 + 顶上又扁又宽的伞盖 + 全屏紫闪

- 新增阳光帝果 👑（sunemperor）：黄色的坚果头上顶着一颗小一号的坚果，两颗的
  左右都长着向日葵花瓣（整株 CSS 画的）。血量 Infinity，谁也啃不动
- 空手点它一下扣 500 阳光：回满血（带一圈金色回血光）+ 在它**前面那一格**
  攒出一颗保龄球，3 秒后顺着这一行滚出去碾人（阳光为 ∞ 时不扣）
- 同时只攒一颗球：滚出去之前再点，是把这一颗撑大（每点 +35%，56px 起步），
  而不是再排一颗；攒球期间球就摆在前面那一格，点一下肉眼可见地大一圈
- 滚出去之后清零，下一颗从最小号重新开始长
- 保龄球换成阳光帝果那一套长相：黄色球身 + 两边各一片向日葵花瓣（`.se-ball`），
  花瓣跟着球一起转
- 阳光帝果 👑 + 绿叶素：五行一起滚出 40 颗**最小号**保龄球，整片草坪一起碾过去
- 3 秒倒计时挂在植物身上按 deltaTime 推进（不是 setTimeout），所以暂停时也跟着停

- 新增红叶素 ❤️（redfood）：红色的爱心，和绿叶素一样是点在植物上用的道具。
  点谁，谁接下来 5 秒打出的子弹就全换成小红樱桃，到点自己恢复原来的样子
- 小红樱桃（redcherry 弹道）：每颗 1000 点伤害 —— 僵尸满血
  800，所以红叶素期间任何植物都能一发一只
- 实现方式是在 `spawnProjectile` 里拦一道：这株植物 `redMs > 0` 就把弹种换成
  redcherry，所以豌豆 / 机枪 / 投手 / 卷心菜……不挑植物，全都吃这个效果
- 计时挂在 `Plant.update` 里按 deltaTime 走（暂停时也跟着停），期间植物罩一层红光

- 新增豌豆炸弹 🌿🌱（peabomb）：左边双重射手的脸、右边豌豆的脸，中间一根樱桃梗
  把两颗头连起来。种下 3 秒后自爆，先在身边围出两圈、共 100 颗豌豆（每圈 50 颗），
  然后每颗沿着自己那条半径朝外射出去，360° 全覆盖
- 豌豆炸弹 + 绿叶素：满地召唤会射豌豆的植物（豌豆 / 双重 / 三重 / 加特林 /
  超级机枪 / 电能机枪），5×9 = 45 格全占满
- 豌豆炸弹 + 红叶素：立刻起爆，两圈射出去的全换成红樱桃子弹（弹种由
  `spawnProjectile` 按 `redMs` 自动换，不用特判）
- 子弹回收补了左边界：以前只判 `x > boardWidth`，往左飞的那一半会一直留在场上

- 新增火炬树桩 🔥（torchwood）：一截树桩顶着一团火焰（都是 CSS 画的）。豌豆类
  子弹（normal / ice / gatling / primitivepea / mgpea / scatterpea）从它这一格
  穿过去会被点着：子弹换成火色，伤害 ×2
- 火炬树桩 🔥 + 绿叶素：火焰升一档，一路走完彩虹七色 —— 红 ×2 → 蓝 ×4 →
  紫 ×8 → 橙 ×16 → 黄 ×32 → 绿 ×64 → 青 ×128，青火到顶，再喂只提示
  「已经是青火了」。植物右上角显示当前倍率徽章
- 同一颗子弹在同一根树桩上只点一次（子弹上记着点过它的树桩 id），
  但连着穿过几根树桩会一根一根叠上去
- 判定放在树桩自己的 tick 里扫场上子弹，Projectile 那边不用改

### 新增房子 🏠 与礼物盒 🎁
- 棋盘左边那条蓝色水岸原来只是块背景，现在在里面排了 3 列 × 5 行的格子
  （`.blue-cell`，棋盘坐标 −260~−20，刚好避开割草机那条道），房子只能种在这儿；
  反过来别的植物也种不进去、房子也种不到草坪上，两边都会给出提示
- 蓝区一格只放一栋，不叠种；`game.blueGrid` 直接存植物本身而不是栈。
  僵尸走到棋盘左边就被割草机清掉，够不着这片区域
- 房子 🏠 + 绿叶素：在草坪上随机挑一格（优先空格）变出礼物盒，落点闪一下金框，
  免得在满屏植物里找不着
- 礼物盒 🎁 空手点一下拆开，变成随机一种植物 —— 池子由 PLANT_EMOJI 自动生成并
  排除房子 / 礼物盒自己 / 绿叶素红叶素铲子三个道具，以后加新植物会自动进池子
- 蓝区也支持铲子、绿叶素、红叶素
- 两者都是 CSS 画的：房子红瓦白墙一窗一门；礼物盒红白斜条纹 + 金丝带蝴蝶结，
  会轻轻左右晃

### 界面：左边水岸的房子（加过又去掉了）
- 一度在左边那片青蓝色地带上画过房子：红瓦屋顶 + 白墙蓝窗 + 石板院子 +
  遮阳伞 / 圆桌 / 烤炉，试过正视、45 度斜视、屋脊竖走几种画法
- 最后按要求整块去掉了；那片地带后来改成了可以盖房子的蓝色格子，见上一节

### 蓝草坪：左边那条青蓝色地带变成能种植物的地
- 棋盘从 9 列扩到 **12 列**，最左边 3 列是「蓝草坪」（`game.blueCols`），
  右边 9 列还是原来的绿草坪
- 蓝草坪和绿草坪完全一样：能种植物、能叠种、能喂绿叶素 / 红叶素、能用铲子，
  僵尸也会一路走过来啃
- 青蓝色背景带跟着铺到蓝草坪的右边缘（`--blue-edge`），蓝草坪的格子加
  `.blue-lawn`：格线偏冷色、棋盘格暗纹换成冷色，和绿草坪区分得开
- 棋盘宽度 720 → 960，仍旧在画布里居中（`--board-x` 自动重算）

### 界面：种子卡排成横着的一行
- 种子卡加到 28 张后，原来 900px 的画布放不下，卡片折成三行把棋盘顶掉了
- 画布加宽到 1280px（`--stage-width`），棋盘居中（`--board-x` 自动算左边距，
  背景那条浅色带跟着棋盘左边缘对齐）
- 顶栏改成两行：第一行阳光 / 音效 / 首页 / 进度条，第二行整条让给种子卡
- 种子卡 `flex-wrap: nowrap` 横着排成一行，卡片从 44×52 缩到 38×46；
  再加卡片的话 `overflow-x: auto` 会变成左右滚动兜底

- 新增西瓜投手 🍉（melonpult）：每 1.2 秒朝同行最靠前的那只僵尸抛一颗西瓜，
  走抛物线从高空越过坚果墙 / 其他僵尸 / 路障 / 铁桶 / 铁门 / 报纸，落地 100 点。
  落点伤害直接扣血，不走 takeDamage 的护盾判定 —— 天上砸下来的东西护甲挡不住
- 西瓜投手 🍉 + 绿叶素：整株变成**西瓜狙击炮**（植株探出炮管、绿色辉光），
  场上有几只僵尸就打几发巨型西瓜，一发 700 点；游荡者打不死，被轰回右侧重爬
- 西瓜投手 🍉 + 红叶素：改打巨型红樱桃，同样是几只僵尸几颗，落地一万点
- 外观：绿条纹西瓜架在木投石座上（CSS 画的），大招时从底下探出炮管；
  飞行中的西瓜 / 红樱桃带拖影，落地各有一圈绿 / 红溅射

- 新增玉米投手 🌽（kernelpult）：每 1.2 秒朝同行最靠前那只僵尸抛一块黄油
  （黄方块），走抛物线越过一切障碍物，落地 50 点伤害，并且黄油黏在僵尸头上
  把它定住 5 秒 —— 头顶顶着一块黄方块，走不动也啃不动
- 玉米投手 🌽 + 绿叶素：场上有几个目标就抛几块巨型黄油，一块 100 点，同样定住 5 秒
- 玉米投手 🌽 + 红叶素：改抛红樱桃，几个目标几颗，落地 1500 点
- 定住复用僵尸自己的 stunTimer，视觉上和眩晕 / 冰冻分开：`.buttered` 排在
  `.stunned` / `.iced` 之后，盖掉金光抖动和 💫，改成头顶一块黄油方块
- 抽出 `classes/Lob.js`：抛物线飞行、跟踪移动目标、落点绕过护盾、齐射错开
  这些西瓜投手和玉米投手共用的逻辑收在一处，两边只描述自己扔什么、砸多疼

- 新增倭瓜 🍈（squash）：带网纹的哈密瓜，睁着两只眼睛蹲在地上，血量 Infinity，谁也啃不动。前方 3 格出现
  僵尸就转过去盯着看 3 秒（整个瓜前倾、眼珠一起转向那只），然后往前跳一格、
  腾空 0.6 秒、砸下来，把落点那一格（连同左右各半格）的僵尸全压扁 —— 一万点
  直接扣血，绕过报纸 / 铁门护盾
- 砸完**不会碎**：在落点歇 0.5 秒，跳回原来那一格重新蹲好，等下一只，可以
  一遍遍来（老家坐标记在 `plant._sqHome` 上）
- 倭瓜 🍈 也砸将王博士：他低头（或被寒冰冻住）、并且左半边落进前方三格时，
  倭瓜就扑上去，落点走 `boss.takeDamage`，一下一万点。他无敌那段时间不会被
  选作目标，免得白跳一趟
- 倭瓜 🍈 + 绿叶素：跳过盯人那 3 秒，立刻起跳
- 状态机 idle → stare → leap → rest → idle 全按 deltaTime 推进，暂停时跟着停

### 每关开打前先选卡
- 新增 `classes/CardPicker.js`：每一关开始前弹出选卡界面，场上有多少张种子卡
  就列多少张（现在 31 张），点一下选中 / 取消，带「全选」「全不选」两个快捷键
- 上限就是全选，下限是**一张都不选**（那一关只能靠割草机和铲子）
- 选中的结果记在 `game.chosenCards`，没选的卡在这一关的种子栏里直接隐藏；
  手上正拿着的卡要是被撤下，会顺手清掉选中状态
- 下一关默认沿用上一关的选择，不用每关从头点
- `Game.start()` 拆成两段：`start()` 只负责弹选卡，选完再走 `beginLevel()`
  跑原来那套开局流程

- 新增核爆樱桃 ☢️（nukecherry）：一颗红樱桃摞在一朵红色的毁灭菇上（蘑菇是 CSS 画的）。
  种下 3 秒自爆，掀起红色巨型蘑菇云（云柱 + 宽伞盖 + 全屏红闪），并从爆心甩出
  一圈红烟
- 红烟以 0.26 px/ms 一直往外扩，不会半路停下 —— 一路扩到能盖住棋盘最远那个角
  才散；扫到的每只僵尸头上再炸一朵小一号蘑菇云，1800 点直接扣血（绕过护盾），
  每只只点名一次；将王博士按他自己的 `takeDamage` 规矩来
- 僵尸体型越大，被点着时甩出来的二次烟越多：普通 1 股、报纸 / 撑杆 2 股、路障 2 股、
  铁桶 3 股、铁门 4 股、将王博士 6 股。二次烟自己也会往外扩、点着路过的僵尸，
  但不再往下连锁（chain=false），另有 28 股的同时上限兜底，免得连锁把帧率拖死
- 小蘑菇云的大小也跟着体型放大
- 核爆樱桃 ☢️ + 绿叶素：满地召唤会爆炸的植物（樱桃 / 土豆 / 毁灭菇 / 豌豆炸弹），
  唯独不召唤它自己

### 开发服务器
- `start.sh` 改名为 `run.sh`，默认动作仍是 start，新增 `restart` 子命令
  （先停再起，端口沿用上次，也可 `./run.sh restart 3000` 换端口）；
  另加 `status` 的端口显示、`-h` 用法、未知参数报错
- 开发服务器强制 `Cache-Control: no-store`：`python3 -m http.server` 不发缓存头，
  浏览器会自作主张缓存 ES 模块，导致改完代码刷新还是旧版本（新植物 / Boss 都看不见）

### 子弹伤害递增（现在只剩超级电能机枪豌豆）
- 伤害递增一度对所有植物生效，现在收窄成**只有超级电能机枪豌豆 ⚡** 吃这一套：
  它打出的每一颗电能弹比上一颗高 15%，复利叠加，封顶 18000
  （`CombatManager` 的 `DAMAGE_GROWTH` / `DAMAGE_CAP` / `GROWS`）
- 其余植物一律用弹种自己的固定伤害，打多久都不涨
- 起点是弹种自己的原始伤害（豌豆 20、卷心菜 40、原始豌豆 70……）；
  秒杀弹（Infinity）不乘倍率，它对博士的 800 折算值在 Projectile 里单独吃倍率
- 计数按株存在 `plant._shots` 上，每株各涨各的，铲掉重种从头来
- 只作用于经 `spawnProjectile` 发射的子弹；全屏随机子弹不传植物，用基础伤害。
  非子弹伤害（樱桃 / 土豆 / 磁力菇引爆 / 玉米炮）不参与

### 将王博士机甲（普通模式 Boss）
- 新增 `classes/Boss.js`：普通 / 融合模式现在只有一个将王博士机甲 🤖，
  体型非常高大（250×470，纵向几乎占满草坪），僵尸全部由他放出，打死他才算赢
- 两阶段循环：放僵尸 10 秒（无敌，每 0.5 秒一只、一波 20 只、按普通 → 路障 →
  铁桶 → 报纸 → 撑杆 → 铁门从弱到强依次出场）→ 低头 8 秒（可被攻击）
- 博士 64000 血，头顶紫色血条 + 数字；进度条改为显示他掉了多少血，
  状态栏显示血量 / 波次 / 当前阶段
- 僵尸血量从 17000 改为 800，每只头顶显示血条与数字（只在掉血时刷新 DOM）
- Infinity 伤害的「秒杀」子弹（水滴 / 黑曜石 / 超级投手 / 黑胶）打博士时
  按 800 折算，否则一发就打光 64000
- 每颗子弹对博士只结算一次伤害：他有 250px 宽，穿透弹飞过去要几十帧，
  不卡这一下的话一颗水滴能连打 40 多次
- 僵尸血条做了反向 scaleX：`.zombie` 整体镜像面朝左，数字会跟着变成「008」
- 去掉「第一局自动通关」：以前首局一开场就 setTimeout 弹胜利画面，
  博士刚出场就被胜利遮罩盖住，等于根本看不见他。现在第一局也正经打，
  点「普通版」的瞬间博士就站在棋盘最右侧开始放僵尸（`firstGame` 标记一并删除）
- 去掉开局第一列自动种的 5 株随机植物：池子里的超级投手 / 胶水 / 黑曜石都是
  秒杀弹（对博士按 800 折算），挂机不动 84 秒就能把博士磨死，等于换了个方式
  自动通关。现在空场开局，输出得自己种
- 博士血量 64000 → 6400 万
- 子弹伤害一度统一成 17000（`BULLET_DAMAGE`），随后又改回各弹种原来的数值：
  豌豆 / 寒冰 / 机枪豌豆 / 散射豌豆 20、卷心菜 40、原始豌豆 70，加特林 / 胶水 /
  黑曜石 / 超级投手 / 水滴 / 保龄球仍是 Infinity「秒杀哨兵」。
  `BOSS_SENTINEL_DAMAGE` 的 800 折算继续生效
- 玉米加农炮的炮弹（1800）、樱桃 / 土豆爆炸、磁力菇与卷心菜大招不属于「子弹」，
  伤害始终没动

### 将王博士的出怪快慢（J / H）
- 新增 `J` 键：将王博士整套节奏 ×3，可无限叠加（`game.bossSpawnRate`）
- `H` 键在原来的「全场僵尸减速 ÷3」之外，顺带把博士的节奏也压成 ÷3
- 倍率同时作用在 spawnTimer 和 phaseTimer 上：放僵尸放得更密，低头也来得更快；
  代价是低头那 8 秒的输出窗口同样按倍率缩短

### 波次（已被博士接管，配置保留）
- 普通模式 9 波全部改为只刷报纸僵尸 📰：每只挡一次伤害，报纸被打掉后 ×2.5 狂暴加速。
  每波总数与出怪间隔沿用原混合波配置（500 / 1100 / 1500 / 2000 / 2100 / 3300 / 3900 / 4700 / 7800）
- 报纸僵尸此前在 9 波配置里一次都没出现过，代码写好了却从未刷出来
- 其余僵尸类型（普通 / 路障 / 铁桶 / 门板 / 撑杆）行为保留在 Zombie.js，只是不再进波次表；
  游荡者模式走 WandererSystem，不受影响

- 超级机枪 🪖：绿叶素大招持续的那 2 秒里，每点这株一下连发数就再 +1，
  点几下加几连（`mgClickUpgrade`，只作用于该格栈顶那株；大招没开时点它没反应）
- 超级机枪 🪖 + 绿叶素：每吃一次，这株的连发数永久 +1（6 → 7 → 8 …），
  植物右上角显示「N 连」徽章。升级存在植物身上，铲掉就没了，同格叠种各算各的；
  一次绿叶素只加一次（大招持续期间不会连加）
- 连发数超过 8 颗后自动另起一条弹链上下铺开，避免弹链一路排到屏幕右边

### 性能
- 修复满屏僵尸时卡到几乎不动：`Plant.takeDamage` 每次被啃都用
  `void offsetWidth` 强制同步布局来重启受击动画，上百只僵尸同时开啃时
  每帧要刷上百次全页布局。实测 287 僵尸 + 满屏叠种下 update 从 544ms/帧
  降到 8.4ms/帧（其中 takeDamage 533ms → 0.4ms）。改为动画正在播时不重启，
  顺带让 plantHit 能完整播完（原来每 16ms 重启，永远播不过 20%）
- 新增同时存活子弹上限 700（`CombatManager.MAX_PROJECTILES`）：帧率一低，
  子弹（每帧走固定像素）就活得更久、越攒越多、越攒越卡，形成正反馈。
  封顶后实测帧率从「8.5 → 4.0 持续下滑」变为稳定在 ~10；正常玩法碰不到这个上限
- spawnProjectile 触顶时返回 null，取返回值的调用方（超级机枪散射、坚果保龄）已判空

## v3.0 - 植物绝技与时间操控（2026-05-03 ~ 2026-05-04）

### 新植物
- 新增原始豌豆 🌟（primitivepea）：70 伤害豌豆，命中眩晕僵尸 2 秒并击退 80px
- 新增三重豌豆射手 🌳（triplepea）：同时向上中下三行各射一颗豌豆
- 新增坚果保龄球 🥥（nutbowling）：放下约 2 秒后沿当前行高速滚动，碾碎沿途所有僵尸
- 新增绿叶素 💚（plantfood）：选中后点向任意植物，触发 2 秒大招（攻速 ×20）

### 绿叶素特殊效果
- 普通发射型植物：进入大招期间无视僵尸检测疯狂开火，timer 累积速度 ×20
- 樱桃 🍒 + 绿叶素：满地 5×9 随机生成 13 种植物
- 土豆 🥔 + 绿叶素：满地铺樱桃，下一帧连锁爆炸
- 坚果保龄球 🥥 + 绿叶素：保龄球滚过每一格自动种下随机土豆 / 樱桃

### 时间操控（敌人加减速）
- 新增 G 键：全场僵尸 ×3 加速（含游荡者，可叠加）
- 新增 H 键：全场僵尸 ÷3 减速（含游荡者，可叠加）

### 平衡与机制调整
- 所有僵尸血量统一从 99999 调整为 17000（仍远高于原版）
- 樱桃炸弹改为约 0.8 秒后引爆（之前是种下立即爆炸）
- 寒冰子弹减速从 50% 改为每次命中 30%（可叠加）
- WaveManager 改用按权重随机选僵尸类型，移除大 pool 数组
- 移除各波次中报纸僵尸出场，替换为路障 / 撑杆

### 无限割草机
- 割草机划出屏幕后自动重置位置复用，不再消失
- triggerLawnmower 找不到可用割草机时即时新建一台，不再判负
- 一行可以无限触发割草机

### UI 改进
- 顶栏由 60px 加高至 120px，腾出空间
- 进度条下方新增波次统计标签：当前波 / 剩余 / 场上僵尸数
- 漫游模式下显示场上僵尸数；全部完成后显示完成提示
- 大招植物添加绿色光晕 + 缩放脉动动画

### 视觉
- 新增坚果保龄球椰子球外观（径向渐变 + 旋转滚动动画）
- 新增原始豌豆金色光球外观

### 工程
- 拼音变量名统一改为英文：yuanshiwandou → primitivepea，sanchongwandousheshou → triplepea，jianguobaolingqiu → nutbowling，lvyesu → plantfood
- 同步重新构建 docs/ 下的 GitHub Pages 部署产物
- 修订首页玩法说明，与代码当前行为对齐（成本、减速比例、引爆时间、绝技效果等）

---

## v2.0 - 天马行空大爆发（2026-05-01 ~ 2026-05-02）

![游戏中](screenshot/v3-gameplay.png)
![游荡者模式](screenshot/v3-wanderer.png)

### 新植物
- 新增寒冰射手 ❄️：子弹命中减速僵尸，可完全冻结游荡者
- 新增双重射手 🌿：同时发射两颗豌豆
- 新增樱桃炸弹 🍒：种下即爆，清除 3×3 范围内所有僵尸
- 新增土豆地雷 🥔：延迟触发，僵尸踩上即秒杀
- 新增投手 🎯：发射穿透型子弹，贯穿整行僵尸
- 新增黏液菇 🧿：子弹命中显著减慢僵尸移速
- 新增黑曜石 🗿：近战超高伤害
- 新增加特林豌豆 🔫：极高射速连射
- 新增水滴射手 💧：水滴子弹
- 新增玉米加农炮 🌽：手动点击瞄准，对目标无限连发；可同时锁定多个目标；暂停时点击僵尸，恢复后延迟爆炸

### 新僵尸
- 新增报纸僵尸：报纸被打掉后暴走加速
- 新增撑杆跳僵尸：跳过第一株植物
- 新增门板僵尸：持门板当盾，超高护甲
- 所有僵尸血量统一调整为 99999

### 游荡者模式
- 全新游荡者模式：10 只 👾 游荡者在场上永久游荡，血量无限大，永远无法被消灭
- 游荡者经过植物格子会造成伤害
- 坚果墙可阻挡游荡者前进（不影响普通僵尸）
- 寒冰子弹可完全冻结游荡者移动
- 游荡者使用 typed array + canvas 独立渲染，支持大量实体

### 融合模式
- 同格反复点击同类植物可升级融合，伤害/射速按融合等级叠加

### 叠种机制
- 同一格可无限叠种，格子显示 ×N 徽章
- 单次种植数量可在 HUD 调整（最高 1,000,000）
- 叠种越多，栈顶植物射速越快

### 操作与快捷键
- 空格键：暂停 / 继续
- `F` 键：植物全体加速 ×3，可无限叠加（×9、×27、×81……）
- `Esc`：取消选植物 / 退出加农炮瞄准模式
- 铲子一次铲除该格所有叠种植物

### 游戏设置
- 新增僵尸速度滑块（很慢 → 噩梦）
- 新增僵尸数量倍率（×0.1 ~ ×10）
- 新增全屏随机子弹开关：开启后每帧在全场随机生成各类子弹
- 阳光、波数、僵尸量均可在开始前配置

### 工程重构
- 代码按功能拆分为 7 个独立模块：`Constants` / `WandererSystem` / `WaveManager` / `UIEvents` / `CombatManager` / `SunManager` / `PlantManager`
- `app.js` 从 1103 行精简至 382 行，Game 类只保留核心生命周期与主循环

---

## v0.3 - 说明与启动（2026-02-09）

![说明界面](screenshot/v2-2a85cd-instruction.png)

- 在开始界面添加「怎么玩」玩法说明
- 添加 `start.sh` 启动脚本

## v0.2 - 核心玩法完善（2026-02-09）

![v0.2 游戏界面](screenshot/v2-2a85cd.png)

- 修复植物射击/产出阳光改为基于 deltaTime，不同刷新率行为一致
- 实现 6 波关卡系统，含进度条显示
- 新增路障僵尸 (200 HP) 和铁桶僵尸 (400 HP)
- 添加割草机防线，每行一个，僵尸突破时自动清场
- 添加铲子工具，可移除已种植的植物
- 添加种子卡冷却系统及视觉遮罩
- 添加胜利画面（击退全部 6 波后触发）
- 添加「阳光不足」/「冷却中」提示
- 坚果墙增强至 400 HP，含 3 阶段受损外观
- 限制 deltaTime 防止切换标签页后时间跳跃

## v0.1 - 初始版本（2026-02-09）

![v0.1 初始界面](screenshot/v1-414b22.png)

- 基础游戏框架：HTML5 + CSS + 原生 JS
- 9x5 网格草坪
- 3 种植物：豌豆射手、向日葵、坚果墙
- 普通僵尸生成
- 阳光掉落与收集机制
- 豌豆子弹碰撞检测
- 开始/游戏结束界面
